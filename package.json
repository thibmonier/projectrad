{
  "name": "projetrad",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bower": "*"
  },
  "engines": {
    "node": "4.0.0"
  },
  "scripts": {
    "postinstall": "bower cache clean && bower install"
  }
}
